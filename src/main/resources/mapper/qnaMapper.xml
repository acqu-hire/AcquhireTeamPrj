<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aqh.board.domain.dao.QnAMapper">
	
	<select id="boardCount" resultType="int">
		SELECT count(*) 
		FROM board 
		WHERE menu='QNA'
	</select>
	
	<update id="readCountUp" parameterType="long">
		UPDATE board 
		SET readCount=readCount+1
		WHERE bNo=#{bNo} AND menu='QNA'
	</update>
	
	<select id="selectAll" resultType="BoardDTO">
		SELECT bNo, title, id, writeDay, readCount
		FROM board 
		WHERE menu='QNA'
		ORDER BY writeDay DESC;
	</select>
	
	<select id="selectDetail" parameterType="long" resultType="BoardDTO">
		SELECT bNo, title, contents, id, writeDay, readCount, file
		FROM board
		WHERE bNo=#{bNo} AND menu='QNA'
	</select>
	
	<insert id="insert" parameterType="BoardDTO">
		INSERT INTO board(id, title, contents, writeDay, menu)
		VALUES (#{id}, #{title}, #{contents}, now(), 'QNA')
	</insert>
	
	<update id="update" parameterType="BoardDTO">
		UPDATE board
		SET title=#{title}, contents=#{contents}
		WHERE bNo=#{bNo} AND menu='QNA'
	</update>
	
	<delete id="deleteAll">
		DELETE FROM board
		WHERE menu='QNA'
	</delete>
	
	<delete id="delete" parameterType="long">
		DELETE FROM board
		WHERE bNo=#{bNo} AND menu='QNA'
	</delete>
</mapper>