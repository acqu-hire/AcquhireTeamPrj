<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aqh.board.domain.dao.QnAMapper">
	<select id="selectAll" resultType="BoardDTO">
		SELECT bNo, title, id, writeDay, readCount
		FROM board 
		WHERE menu='QNA'
	</select>
	<select id="selectDetail" parameterType="int" resultType="BoardDTO">
		SELECT bNo, title, contents, id, writeDay, readCount, file
		FROM board
		WHERE bNo=#{bNo} AND menu='QNA'
	</select>
</mapper>