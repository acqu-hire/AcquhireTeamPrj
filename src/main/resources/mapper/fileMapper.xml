<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aqh.common.domain.dao.FileMapper">
	<insert id="upload" parameterType="java.util.List">
		INSERT INTO attachfile
		(
			bNo, 
			uuid, 
			originName, 
			uploadPath, 
			fileSize, 
			fmtFileSize
		)
		VALUES
		<foreach collection="list" item="files" separator=",">
		(
			#{files.bNo}, 
			#{files.uuid}, 
			#{files.originName}, 
			#{files.uploadPath}, 
			#{files.fileSize}, 
			#{files.fmtFileSize}
		)
		</foreach>
	</insert>
	
	<delete id="delete">
		DELETE FROM attachfile 
		WHERE fNo IN
		<foreach collection="array" item="fNo" open="(" close=")" separator=",">
		${fNo}
		</foreach>
	</delete>
	
	<select id="fileList" resultType="FileDTO">
		SELECT * FROM attachfile
		WHERE fNo IN
		<foreach collection="array" item="fNo" open="(" close=")" separator=",">
		${fNo}
		</foreach>
	</select>
	
	<select id="fileDetail" parameterType="int" resultType="FileDTO">
		SELECT * FROM attachfile
		WHERE fNo = ${fNo}
	</select>
</mapper>